<input 
    type="file" 
    name="{{ $name ?? '' }}" 
    id="{{ $id ?? '' }}"
    accept="{{ $accept ?? '' }}"
    class="appearance-none bg-white border border-gray-300 rounded-md py-2 px-4 text-sm leading-tight focus:outline-none focus:bg-white focus:border-blue-500">
